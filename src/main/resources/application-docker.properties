# Docker profile overrides

# PostgreSQL - connect to docker compose service by name
spring.datasource.url=jdbc:postgresql://postgres:5432/student_data_processor
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:postgres}

# Output directory inside container
app.output.directory=/data/output/
